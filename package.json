{
  "name": "package.json",
  "version": "1.0.0",
  "description": "package.json file for assignment1",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "compile": "babel src --presets react,es2015 --out-dir public",
    "watch": "babel src --presets react,es2015 --out-dir public -watch --verbose",
    "start": "node server/server.js"
  },
  "author": "Mohit Agarwal",
  "license": "ISC",
  "dependencies": {
    "express": "^4.17.1",
    "webpack": "^5.19.0"
  },
  "devDependencies": {
    "babel-cli": "^6.26.0",
    "babel-preset-es2015": "^6.24.1",
    "babel-preset-react": "^6.24.1",
    "nodemon": "^2.0.7"
  }
}
